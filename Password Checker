username = input("Enter username: ")
password = input("Enter password: ")
pwd_len = len(password)

# Print the username and have the password blocked
print(f'{username}, your password {password} is {pwd_len} letters long')
